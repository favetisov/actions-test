# This is a basic workflow to help you get started with Actions

name: scheduler test

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the master branch
on:
  release:
     types: [ published ] 
      

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
    - uses: actions/checkout@v2

    - name: Telegram Message Notify
      uses: appleboy/telegram-action@0.0.7
      with:
        token: ${{ secrets.TG_BOT_TOKEN }}        
        to: ${{ secrets.TG_CICD_CHANNEL_ID }}
        format: markdown
        message: |
          **FTB-ADMIN**
          Release ${{ GITHUB_REF }} is building and shipping to production
    
    - name: Build
      run: echo "I'm building"
      
    - name: Build Failure
      if: ${{ failure() }}
      uses: appleboy/telegram-action@0.0.7
      with:
        token: ${{ secrets.TG_BOT_TOKEN }}        
        to: ${{ secrets.TG_CICD_CHANNEL_ID }}
        format: markdown
        message: |
          **FTB-ADMIN**
          ⚠️ Release ${{ GITHUB_REF }} has failed at build state ⚠️
                
    - name: Deploy
      if: ${{ success() }}
      run: echo "I'm deploying"
      
    - name: Build
      run: echo "I'm building"
      
    - name: Deploy Failure
      if: ${{ failure() }}
      uses: appleboy/telegram-action@0.0.7
      with:
        token: ${{ secrets.TG_BOT_TOKEN }}        
        to: ${{ secrets.TG_CICD_CHANNEL_ID }}
        format: markdown
        message: |
          **FTB-ADMIN**
          ⚠️ Release ${{ GITHUB_REF }} has failed at deploy state ⚠️
      
    - name: Deploy Success
      if: ${{ success() }}
      uses: appleboy/telegram-action@0.0.7
      with:
        token: ${{ secrets.TG_BOT_TOKEN }}        
        to: ${{ secrets.TG_CICD_CHANNEL_ID }}
        format: markdown
        message: |
          **FTB-ADMIN**
          ✅ Release ${{ GITHUB_REF }} landed on production 🎉
          
      
